name: Build iOS App (Xcode Project)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true


    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Create Xcode Project and Source Files
      run: |
        mkdir ProximityApp
        cd ProximityApp

        # ViewController.swift
        cat << 'EOF' > ViewController.swift
        import UIKit

        class ViewController: UIViewController {
            override func viewDidLoad() {
                super.viewDidLoad()
                UIDevice.current.isProximityMonitoringEnabled = false
            }
        }
        EOF

        # AppDelegate.swift
        cat << 'EOF' > AppDelegate.swift
        import UIKit

        @main
        class AppDelegate: UIResponder, UIApplicationDelegate {
            var window: UIWindow?
            func application(_ application: UIApplication,
                didFinishLaunchingWithOptions launchOptions:
                [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
                return true
            }
        }
        EOF

        # Info.plist
        cat << 'EOF' > Info.plist
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
        "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>CFBundleIdentifier</key>
            <string>com.example.ProximityApp</string>
            <key>CFBundleName</key>
            <string>ProximityApp</string>
            <key>CFBundleVersion</key>
            <string>1</string>
            <key>CFBundleShortVersionString</key>
            <string>1.0</string>
            <key>UILaunchStoryboardName</key>
            <string>LaunchScreen</string>
            <key>UIMainStoryboardFile</key>
            <string>Main</string>
        </dict>
        </plist>
        EOF

        # Storyboards
        mkdir -p Base.lproj

        # Main.storyboard
        cat << 'EOF' > Base.lproj/Main.storyboard
        <?xml version="1.0" encoding="UTF-8"?>
        <document type="com.apple.InterfaceBuilder3.CocoaTouch.Storyboard.XIB"
        version="3.0" toolsVersion="22000" targetRuntime="iOS.CocoaTouch"
        initialViewController="VC">
        <scenes>
            <scene sceneID="main">
                <objects>
                    <viewController id="VC" customClass="ViewController">
                        <view key="view" id="view">
                            <rect key="frame" x="0" y="0" width="393" height="852"/>
                            <color key="backgroundColor" white="1" alpha="1"/>
                        </view>
                    </viewController>
                </objects>
            </scene>
        </scenes>
        </document>
        EOF

        # LaunchScreen.storyboard
        cat << 'EOF' > Base.lproj/LaunchScreen.storyboard
        <?xml version="1.0" encoding="UTF-8"?>
        <document type="com.apple.InterfaceBuilder3.CocoaTouch.Storyboard.XIB"
        version="3.0" toolsVersion="22000" targetRuntime="iOS.CocoaTouch"
        launchScreen="YES">
        <scenes>
            <scene sceneID="launch">
                <objects>
                    <viewController id="LS">
                        <view key="view" id="view">
                            <rect key="frame" x="0" y="0" width="393" height="852"/>
                            <color key="backgroundColor" white="1" alpha="1"/>
                        </view>
                    </viewController>
                </objects>
            </scene>
        </scenes>
        </document>
        EOF

        # Create Xcode project
        xcodebuild -create-xcodeproj -project ProximityApp.xcodeproj \
          -target ProximityApp -type app \
          -sources ViewController.swift AppDelegate.swift \
          -plist Info.plist \
          -resources Base.lproj

    - name: Build App
      run: |
        xcodebuild -project ProximityApp/ProximityApp.xcodeproj \
          -scheme ProximityApp -sdk iphoneos -configuration Release build

    - name: Package IPA
      run: |
        mkdir Payload
        cp -R ProximityApp/build/Release-iphoneos/ProximityApp.app Payload/
        zip -r ProximityApp-Xcode.ipa Payload

    - name: Upload IPA
      uses: actions/upload-artifact@v4
      with:
        name: ProximityApp-Xcode
        path: ProximityApp-Xcode.ipa
